# ============================================
# Variables de Entorno - AutomatizaciÃ³n IT
# ============================================
# 
# INSTRUCCIONES:
# 1. CopiÃ¡ este archivo como .env
# 2. CompletÃ¡ los valores con tus credenciales
# 3. El archivo .env estÃ¡ en .gitignore (no se sube al repositorio)
#
# cp .env.example .env
# ============================================

# ============================================
# CREDENCIALES WINRM (Windows Remote Management)
# ============================================
# Usuario para conexiones WinRM
WINRM_USER=

# ContraseÃ±a para conexiones WinRM
WINRM_PASS=

# Dominio (opcional, ej: DOMINIO o dejar vacÃ­o)
WINRM_DOMAIN=

# Usar SSL/TLS (true/false). Por defecto: false (puerto 5985)
# Si es true, usa puerto 5986
WINRM_SSL=false

# MÃ©todo de autenticaciÃ³n: auto, kerberos, ntlm, credssp, basic
# Por defecto: auto (detecta automÃ¡ticamente)
WINRM_AUTH=auto

# ============================================
# CREDENCIALES PSEXEC (Sysinternals PsExec)
# ============================================
# Usuario para conexiones PsExec
# Si no se especifica, usa WINRM_USER como fallback
PSEXEC_USER=

# ContraseÃ±a para conexiones PsExec
# Si no se especifica, usa WINRM_PASS como fallback
PSEXEC_PASS=

# Dominio para PsExec (opcional)
# Si no se especifica, usa WINRM_DOMAIN como fallback
PSEXEC_DOMAIN=

# Encriptar comunicaciÃ³n PsExec (true/false). Por defecto: false
PSEXEC_ENCRYPT=false

# Usar cuenta del sistema (true/false). Por defecto: true
PSEXEC_SYSTEM=true

# ============================================
# NOTAS IMPORTANTES
# ============================================
# - Las credenciales pueden compartirse entre WinRM y PsExec
# - Si solo configurÃ¡s WINRM_USER/WINRM_PASS, PsExec las usarÃ¡ automÃ¡ticamente
# - Si configurÃ¡s ambas, cada una usarÃ¡ sus propias credenciales
# - DejÃ¡ vacÃ­o el dominio si no usÃ¡s dominio (workgroup)
# - El archivo .env NO debe subirse al repositorio (ya estÃ¡ en .gitignore)
